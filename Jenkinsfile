pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git 'https://github.com/nunidoron/test3107.git'
                sh 'hello.py'
            }
        }
    }
}
